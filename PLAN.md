# PLAN

## 0. Контекст

- Проект: MCP-сервер с Corrective RAG.
- База знаний: тактика на боссов в Path of Exile 2.
- Темы данных: фазы, способности, окна урона, позиционирование, контрмеры.

## 1. Цели плана

- Реализовать 4 MCP-инструмента по требованиям.
- Поднять гибридный retrieval (BM25 + vector + RRF).
- Реализовать Corrective RAG граф в LangGraph с retry до 2.
- Подготовить тесты, docker compose, документацию.

## 3. Этапы реализации

## Этап 1. Каркас проекта и конфиг

Входы:
- REQUIREMENTS.md
- AGENTS.md

Выходы:
- Структура директорий
- Конфиг приложения (пути индекса, модели)
- Базовый MCP сервер с health init

Definition of Done:
- Проект запускается локально.
- Конфиг читается из env/файла.

Риски:
- Расхождение стека с требованиями.

Rollback:
- Оставить минимальный каркас и зафиксировать несовместимые решения в отчете.

## Этап 2. Индексатор документов

Входы:
- DATA SPEC

Выходы:
- Загрузка файлов поддерживаемых форматов
- Чанкинг
- Запись в Chroma
- Метаданные чанков
- Инкрементальная переиндексация

Definition of Done:
- `index_folder` индексирует `sample_docs`.
- `index_status` показывает валидные счетчики.

Риски:
- Нестабильный парсинг json/yaml.

Rollback:
- Fallback на plain-text и лог parse_warnings.

## Этап 3. Retrieval слой

Входы:
- RETRIEVAL SPEC

Выходы:
- BM25 индекс (самописная Okapi реализация)
- Vector retrieval
- RRF объединение
- Дедуп
- ranking trace

Definition of Done:
- `find_relevant_docs` возвращает ранжированные чанки и trace.

Риски:
- Дубли и нестабильный порядок.

Rollback:
- Упростить веса и усилить tie-break правила.

## Этап 4. Graph Corrective RAG

Входы:
- GRAPH SPEC

Выходы:
- Узлы rewrite/retrieve/grade/decision/broaden/generate
- Retry до 2
- Таймауты и fallback

Definition of Done:
- `ask_question` возвращает ответ + источники.
- Цикл retry работает по правилам.

Риски:
- LLM таймауты.

Rollback:
- Деградированный режим: extractive answer из top chunks.

## Этап 5. Тесты

Входы:
- TEST SPEC

Выходы:
- Минимум 10 тестов (план: 12)
- Unit + e2e

Definition of Done:
- Все тесты проходят локально.
- CI запускает lint + test.

Риски:
- flaky e2e на локальной модели.

Rollback:
- Для CI использовать mock LLM в e2e-lite.

## Этап 6. Инфраструктура и документация

Входы:
- Все предыдущие этапы

Выходы:
- Docker Compose
- README
- ARCHITECTURE
- MCP config пример

Definition of Done:
- `docker compose up` поднимает сервисы.
- Проверочный сценарий из требований выполняется.

Риски:
- Версионные конфликты контейнеров.

Rollback:
- Зафиксировать working версии в compose и lock-файлах.

## 4. План данных по тактикам на боссов в Path of Exile 2

- `sample_docs/bosses/`:
  - боссы, их абилки, фазы, ключевые тайминги и телеграфы.
- `sample_docs/lore_facts/`:
  - специально добавленные проверочные факты для RAG-проверки.

## 5. Критерии готовности плана

- План покрывает все обязательные требования.
- Есть этапы для тестов, инфраструктуры и документации.
- Можно переходить к реализации этапа 1 после подтверждения.

## 6. План закрытия пробелов перед сдачей

Цель:
- Закрыть расхождения между текущей реализацией и обязательными пунктами REQUIREMENTS.

### 6.1 Corrective RAG граф (обязательно)

Статус:
- Выполнено.

Что сделано:
- Узлы вынесены в LangGraph: `rewrite`, `retrieve`, `grade`, `broaden`, `generate`.
- Условный переход в `addConditionalEdges`: при нехватке релевантных чанков идем в `broaden` и повторяем retrieval.
- Retry ограничен `MCP_GRAPH_MAX_RETRIES` (по умолчанию 2), счетчик и трасса возвращаются в ответе.

Definition of Done:
- `ask_question` проходит сценарии: достаточная релевантность, недостаточная релевантность, деградация при ошибке LLM.
- В ответе есть `attempts`, `rewrittenQuery`, `sources`.

### 6.2 Retrieval до требуемого уровня (обязательно)

Статус:
- Выполнено.

Что сделано:
- Гибридный поиск BM25 + vector + RRF сохранен.
- BM25 реализован как самописный Okapi BM25 модуль в Node.js (без внешней BM25-библиотеки).
- Vector retrieval использует Chroma (in-process режим, встроенная БД) через JS SDK (`chromadb`) + deterministic embedding.
- Трассировка ранжирования сохраняется: `bm25Hits`, `vectorHits`, `rrfCandidates`, `finalOrder`.

Definition of Done:
- `find_relevant_docs` возвращает ранжированные чанки и trace для смешанных (keyword + semantic) запросов.
- Тесты покрывают поведение retrieval на ключевых фактах из базы.

### 6.3 Набор документов >= 500kb (обязательно)

Статус:
- Выполнено ранее (датасет расширен).

Definition of Done:
- Общий размер файлов в `sample_docs` не менее 500kb.
- Проверочные факты из README находятся retrieval и `ask_question`.

### 6.4 Сценарий приемки с пустым индексом (обязательно)

Статус:
- Выполнено ранее.

Definition of Done:
- Первый `index_status()` после чистого старта возвращает пустое состояние.
- После `index_folder("./sample_docs")` счетчики заполняются корректно.

### 6.5 Приемочные проверки и стабилизация (обязательно)

Статус:
- Выполнено.

Что сделано:
- Обновлены тесты под финальный граф и retrieval.
- Прогоны `npm run check`, `npm run lint`, `npm run test` проходят.
- `docker-compose up --build -d` поднимает инфраструктуру.

Definition of Done:
- Все проверки проходят локально.
- Сценарий из REQUIREMENTS воспроизводится без ручных обходных действий.

### 6.6 Порядок выполнения

1. Граф Corrective RAG.
2. Эмбеддинги + Chroma для vector retrieval.
3. Расширение `sample_docs` до 500kb.
4. Чистый старт индекса и фиксация шагов в README.
5. Финальные тесты и демо-прогон.

## 7. Спецификация соответствия стеку Node.js

Решения для формально-пакетных требований:
- `rank_bm25`: в Node.js используется самописная Okapi BM25 реализация, функционально закрывающая sparse retrieval.
- ChromaDB (in-process): встроенная векторная БД, хранится локально в `./data/chroma`, не требует отдельного сервера.

Критерий приемки для этого раздела:
- Функционально выполняются требования к гибридному retrieval (BM25 + vector + RRF) и Corrective RAG без смены Node.js-стека.
